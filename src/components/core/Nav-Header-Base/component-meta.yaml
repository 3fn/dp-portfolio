purpose: "Structural primitive for top-of-screen navigation bars providing safe area, background, layout regions, and landmark semantics. Internal only — use Nav-Header-Page or Nav-Header-App."

# Derived from family-level guidance (Component-Family-Navigation.md)
usage:
  when_to_use:
    - "Building a semantic header variant that needs safe area, background, layout regions, and landmark semantics"
    - "Internal composition target for Nav-Header-App and Nav-Header-Page"
  when_not_to_use:
    - "Product-level screen headers — use Nav-Header-App (root destinations) or Nav-Header-Page (pushed/presented screens)"
    - "Direct use in product code — this is an internal structural primitive"

contexts:
  - "app-bars"

alternatives: []
