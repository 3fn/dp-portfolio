purpose: "Represent a single option within a radio group for mutually exclusive choices."

usage:
  when_to_use:
    - "Individual radio options inside an Input-Radio-Set"
    - "Mutually exclusive choices where only one can be selected"
  when_not_to_use:
    - "Standalone boolean choices — use Input-Checkbox-Base"
    - "Multi-select choices — use Input-Checkbox-Base (multiple instances)"
    - "Using outside an Input-Radio-Set container"

contexts:
  - "forms"
  - "settings-screens"

alternatives:
  - component: Input-Text-Email
    reason: "When email address collection"
  - component: Input-Text-Password
    reason: "When password entry"
  - component: Input-Text-PhoneNumber
    reason: "When phone number collection"
  - component: Input-Text-Base
    reason: "When generic text input"
  - component: Input-Checkbox-Legal
    reason: "When terms of service acceptance"
  - component: Input-Checkbox-Base
    reason: "When generic binary selection"
